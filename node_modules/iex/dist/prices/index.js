"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
var client_1 = require("../client");
var iex_request_1 = __importDefault(require("../iex-request"));
var utils_1 = require("../utils");
function history(settings) {
    utils_1.Effect(client_1.validateSettings).run(settings);
    return function requestHistory(symbol, params, range) {
        return iex_request_1.default(settings, "/stock/" + symbol + "/chart" + (range ? "/" + range : ''), params);
    };
}
exports.history = history;
function intraday(settings) {
    utils_1.Effect(client_1.validateSettings).run(settings);
    return function requestIntraday(symbol, params) {
        return iex_request_1.default(settings, "/stock/" + symbol + "/intraday-prices", params);
    };
}
exports.intraday = intraday;
function previous(settings) {
    utils_1.Effect(client_1.validateSettings).run(settings);
    return function requestPrevious(symbol, params) {
        return iex_request_1.default(settings, "/stock/" + symbol + "/previous", params);
    };
}
exports.previous = previous;
function price(settings) {
    utils_1.Effect(client_1.validateSettings).run(settings);
    return function requestPrice(symbol, params) {
        return iex_request_1.default(settings, "/stock/" + symbol + "/price", params);
    };
}
exports.price = price;
function quote(settings) {
    utils_1.Effect(client_1.validateSettings).run(settings);
    return function requestQuote(symbol, params, field) {
        return iex_request_1.default(settings, "/stock/" + symbol + "/quote" + (field ? "/" + field : ''), params);
    };
}
exports.quote = quote;
var PricesClient = /** @class */ (function () {
    function PricesClient(settings) {
        utils_1.Effect(client_1.validateSettings).run(settings);
        this.history = history(settings);
        this.intraday = intraday(settings);
        this.previous = previous(settings);
        this.price = price(settings);
        this.quote = quote(settings);
    }
    PricesClient.create = function (settings) {
        return new PricesClient(settings);
    };
    return PricesClient;
}());
exports.default = PricesClient;
module.exports = PricesClient;
