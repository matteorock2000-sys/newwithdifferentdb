"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
var client_1 = require("../client");
var iex_request_1 = __importDefault(require("../iex-request"));
var utils_1 = require("../utils");
function balanceSheet(settings) {
    utils_1.Effect(client_1.validateSettings).run(settings);
    return function requestBalanceSheet(symbol, params) {
        return iex_request_1.default(settings, "/stock/" + symbol + "/balance-sheet", params);
    };
}
exports.balanceSheet = balanceSheet;
function cashFlow(settings) {
    utils_1.Effect(client_1.validateSettings).run(settings);
    return function requestCashFlow(symbol, params) {
        return iex_request_1.default(settings, "/stock/" + symbol + "/cash-flow", params);
    };
}
exports.cashFlow = cashFlow;
function dividends(settings) {
    utils_1.Effect(client_1.validateSettings).run(settings);
    return function requestDividends(symbol, params, range) {
        return iex_request_1.default(settings, "/stock/" + symbol + "/dividends" + (range ? "/" + range : ''), params);
    };
}
exports.dividends = dividends;
function earnings(settings) {
    utils_1.Effect(client_1.validateSettings).run(settings);
    return function requestEarnings(symbol, params, last, field) {
        return iex_request_1.default(settings, "/stock/" + symbol + "/earnings" + (last ? "/" + last : '') + (field ? "/" + field : ''), params);
    };
}
exports.earnings = earnings;
function financials(settings) {
    utils_1.Effect(client_1.validateSettings).run(settings);
    return function requestFinancials(symbol, params) {
        return iex_request_1.default(settings, "/stock/" + symbol + "/financials", params);
    };
}
exports.financials = financials;
function reportedFinancials(settings) {
    utils_1.Effect(client_1.validateSettings).run(settings);
    return function requestReportedFinancials(symbol, params, filing) {
        return iex_request_1.default(settings, "/time-series/reported_financials/" + symbol + (filing ? "/" + filing : ''), params);
    };
}
exports.reportedFinancials = reportedFinancials;
function income(settings) {
    utils_1.Effect(client_1.validateSettings).run(settings);
    return function requestIncome(symbol, params) {
        return iex_request_1.default(settings, "/stock/" + symbol + "/income", params);
    };
}
exports.income = income;
function splits(settings) {
    utils_1.Effect(client_1.validateSettings).run(settings);
    return function requestSplits(symbol, params, range) {
        return iex_request_1.default(settings, "/stock/" + symbol + "/splits" + (range ? "/" + range : ''), params);
    };
}
var FundamentalsClient = /** @class */ (function () {
    function FundamentalsClient(settings) {
        utils_1.Effect(client_1.validateSettings).run(settings);
        this.balanceSheet = balanceSheet(settings);
        this.cashFlow = cashFlow(settings);
        this.dividends = dividends(settings);
        this.earnings = earnings(settings);
        this.financials = financials(settings);
        this.reportedFinancials = reportedFinancials(settings);
        this.income = income(settings);
        this.splits = splits(settings);
    }
    FundamentalsClient.create = function (settings) {
        return new FundamentalsClient(settings);
    };
    return FundamentalsClient;
}());
exports.FundamentalsClient = FundamentalsClient;
exports.default = FundamentalsClient;
module.exports = FundamentalsClient;
