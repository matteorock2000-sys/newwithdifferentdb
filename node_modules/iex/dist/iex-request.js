"use strict";
var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
Object.defineProperty(exports, "__esModule", { value: true });
var ky = require('ky-universal');
var defaults_1 = require("./defaults");
function iexRequest(settings, path, params) {
    return ky("https://" + settings.env + ".iexapis.com/" + settings.version + path, __assign(__assign({}, defaults_1.defaultRequestOptions), { searchParams: __assign(__assign(__assign({}, defaults_1.defaultSearchParams), { token: settings.token }), params) })).then(function (resp) { return (params && params.format && params.format !== 'json')
        ? resp.text()
        : resp.json(); });
}
exports.default = iexRequest;
